# Gapsi Frontend (React + TypeScript + Vite + PWA)

## 🚀 Descripción
Aplicación frontend del examen técnico **Gapsi e-Commerce**, desarrollada en **React + TypeScript + Vite** bajo arquitectura **feature-based**.

Incluye PWA, conexión directa con backend en Java Spring Boot (http://localhost:8080/api), Material UI y React Query.

## ⚙️ Requisitos previos
- Node.js v18+
- Backend `gapsi-backend` corriendo en `http://localhost:8080`

## 🧩 Instalación y ejecución
```bash
npm install
npm run dev
```
La aplicación estará disponible en `http://localhost:5173`

## 🧠 Arquitectura
Estructura **feature-based** con patrones `Container-Presenter` y `Custom Hook`.
